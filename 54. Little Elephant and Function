https://codeforces.com/problemset/problem/221/A

#include <bits/stdc++.h>
using namespace std;
int main() {
  int n;
  cin>>n;
  vector <int> arr(1);
  arr[0]=n;
  for (int i=1;i<n;i++){
    arr.push_back(i);
  }
  for (int x: arr){
    cout<<x<<" ";
  }
}
